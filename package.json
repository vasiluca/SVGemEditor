{
  "scripts": {
    "build": "pnpm --filter svgem-react build",
    "dev": "pnpm --filter svgem-react dev",
    "test": "jest",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "lefthook": "^2.0.9"
  }
}
